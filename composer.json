{
    "name": "snip/url-shortener",
    "description": "Snip — URL shortener with Stripe subscriptions",
    "require": {
        "php": ">=8.1",
        "stripe/stripe-php": "^16.0"
    },
    "config": {
        "optimize-autoloader": true
    }
}
