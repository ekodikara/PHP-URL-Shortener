# Local secrets / credentials — never commit
CREDENTIALS.local.md
*.local.md
*.local.txt
.env
.env.*
!.env.example

# Runtime
cache/

# Composer dependencies (installed in the Docker image)
vendor/

# OS / editor
.DS_Store
*.swp
.lighthouseci/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
docs/_render*
.phpunit.result.cache
